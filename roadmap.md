# 🗺️ LA PIRAMIDE PERDUTA — Roadmap di Integrazione Professionale

> **Documento vivo**: aggiornato ad ogni implementazione. Data creazione: 2026-08-13.
> Risorse verificate online (tutti gli URL rispondono 200). Obiettivo: portare il
> vertical slice a livello professionale (asset, audio, VFX, polish) chiudendo i gap
> G-12 → G-19 e i residui G-03 → G-07.
>
> ⚠️ **Stato globale (aggiornato 2026-08-17)**: TUTTI i gap G-01→G-23 chiusi o
> deliberatamente rinviati (unica eccezione: simulazione su Web Worker — vedi §5).
> Gate: `npm run verify` verde (401 test = 351 node + 50 DOM, 16/16 property),
> `npm run test:e2e` 11/11 verde, browser WebGPU: bloom attivo + 12/12 GLB meshopt.

---

## 📌 LEGENDA STATO
- ✅ **Chiuso** — implementato e verificato
- 🟡 **Parziale** — funziona ma con residui noti
- ⬜ **Da fare** — non ancora implementato
- 🔄 **In corso** — sto implementando ora

---

## 1. MAPPA DEI GAP E RISORSE (cosa manca → con cosa si colma)

### 1.1 Codice / Gameplay (nessuna risorsa esterna)

| Gap | Stato | Cosa manca | Nota |
|---|---|---|---|
| G-03 | ✅ | Combattimento completo: HurtboxStore, EnemyAttackResolver, GenericEncounterRuntime per TUTTI gli archetipi, parry system, spawn dinamico Director | Chiuso 2026-08-14/16 (resta calibrazione playtest A-03) |
| G-04 | ✅ | Consumer AI generalizzati del rumore; ricompense scavo; impatto bracieri su Director | `noiseStimulus` implementato in `GenericEncounterRuntime.tick()` — A-01 chiuso |
| G-05 | ✅ | Graft equipaggiabili (UpgradeResolver), 8/8 nodi Ka con effetto, oro→Frammenti, meta-loop multi-floor | Chiuso 2026-08-14 |
| G-06 | ✅ | 17 SFX Kenney reali + 13 cue sintetici + drone ambience + musica adattiva 3 stati | Chiuso 2026-08-14/16 |
| G-07 | ✅ | torchToggle; amplifiedTelegraphs su nemici reali (×1.9 su scale + emissive) | 14/14 attive; `ThreeRendererService.setEnemyStates` applica `telegraphGain` — A-02 chiuso |
| G-22 | ✅ | E2E run-completion, seed variance, combattimento reale | 11/11 verdi (riproducibilità, varianza, fallback WebGL2) |

### 1.2 Asset 3D (G-13, G-14)

| # | Risorsa | URL | Licenza | Uso | Stato |
|---|---|---|---|---|---|
| A1 | ~~Quaternius Modular Ruins~~ → **Ambiente piramide procedurale** | — | — | Colonne papiriformi, soglie dorati, false porte, soffitto stellato — **non** pack Medieval | ✅ sostituito (2026-08-25) |
| A2 | **ToxSam/open-source-3D-assets** (registry GLB) | github.com/ToxSam/open-source-3D-assets | CC0 | 991+ GLB CC0 incl. **Egyptian temples**; JSON per il manifest | ✅ 5/5 landmark GLB integrati |
| A3 | **Sketchfab** (search `egyptian`, filtro CC0/CC-BY) | sketchfab.com/search?q=egyptian&type=models | CC0/CC-BY | Sarcofagi, statue Anubi/Bastet/Thoth/Sobek, obelischi, vasi canopi, bracieri | 🟡 opzionale (ToxSam copre il VS) |
| A4 | **Mixamo** (animazioni) | mixamo.com | Gratis | Idle/walk/attack/death/hit per player viewmodel + PRIEST/ROYAL_MUMMY | ⬜ da integrare |
| A5 | **Kenney — filler pietra neutro** | kenney.nl/assets/mini-dungeon | CC0 | Solo pot/rocks/stairs (`-KenneyStoneOnly`) — **no** barrel/gate/banner | 🟡 opzionale |
| A6 | ~~Quaternius Creatures~~ | — | — | Nemici già da ToxSam/Poly Pizza GLB | ✅ coperto |

### 1.3 Texture / Materiali PBR (G-14, G-16)

| # | Risorsa | URL | Licenza | Uso | Stato |
|---|---|---|---|---|---|
| T1 | **ambientCG** (search `sand`, `rock`, `stone`) | ambientcg.com | CC0 | Texture PBR 4K/8K: sabbia (floor), pietra calcarea (muri), arenaria | ✅ Ground054 + Rock064 (8 file PBR, `public/textures/`) |
| T2 | **Poly Haven** (texture + HDRI) | polyhaven.com/textures, polyhaven.com/hdris | CC0 | HDRI "Sand Dunes"/"Desert" per ambient/reflection | ⬜ da integrare |
| T3 | **OpenGameArt — ancient-egypt-0** | opengameart.org/content/ancient-egypt-0 | CC0 | **Geroglifici (3 fogli), papiro** → landmark + HUD | ✅ 4 texture in `public/textures/` (hieroglyphs_a_m, hieroglyphs_s_l, all_hieroglyphs, papyrus) |
| T4 | **OpenGameArt — egyptian-rpg** | opengameart.org/content/egyptian-rpg | CC0 | Texture/pattern egizi aggiuntivi | ⬜ opzionale |

### 1.4 Audio (G-18, G-19)

| # | Risorsa | URL | Licenza | Uso | Stato |
|---|---|---|---|---|---|
| S1 | **Kenney — Audio Packs** | kenney.nl/assets?q=audio | CC0 | SFX UI (click/hover), impact, hit | ✅ 17 OGG + 13 RPG foley in `public/audio/` (`AudioAssetLibrary.ts`) |
| S2 | **Sonniss — GDC Game Audio Bundle** | sonniss.com/gameaudiogdc | Gratis (registrazione) | Foley professionale: fiamme, pietra, cripta, creature (GB di sample) | ⬜ da scaricare |
| S3 | **freesound** (search `torch fire`, `stone scrape`, `mummy`, `snake hiss`, `sand footsteps`) | freesound.org | CC0/CC-BY | Foley specifico: passi sabbia, sibilo cobra, crepitio torcia | ⬜ da selezionare |
| S4 | **OpenGameArt — desert ambience** | opengameart.org (search `desert ambience`) | CC0/CC-BY | Loop ambience per i 4 livelli di DARKNESS.thresholds | ⬜ da integrare |
| S5 | **Mixkit / Pixabay Music** | mixkit.co, pixabay.com/music | Gratis | Musica dark/egizia per stati (esplorazione/tensione/combattimento) | ⬜ da selezionare |
| S6 | **Web Audio API nativo** (già in uso) | — | — | Sintesi procedurale complementare (fiamma, droni, LFO tensione) | 🟡 estendere |

### 1.5 Font / UI (G-09, G-12)

| # | Risorsa | URL | Licenza | Uso | Stato |
|---|---|---|---|---|---|
| F1 | **Cinzel** | fonts.google.com/specimen/Cinzel | OFL | Titoli/menu — serif epigrafico | ✅ `@fontsource/cinzel` (400+700) importato in `main.ts`, self-hosted |
| F2 | **Noto Sans Egyptian Hieroglyphs** | fonts.google.com/noto/specimen/Noto+Sans+Egyptian+Hieroglyphs | OFL | Geroglifici reali in UI/messaggi | ✅ `@fontsource/noto-sans-egyptian-hieroglyphs` importato in `main.ts` |
| F3 | **fontsource** (self-host) | fontsource.org/fonts/cinzel | OFL | Self-hosting senza CDN (privacy/offline) | ✅ già usato — entrambi i font via `@fontsource/*`, Vite li bundla con i WOFF2 |

### 1.6 VFX / Post-processing (G-15)

| # | Tecnica | Implementazione | Uso | Stato |
|---|---|---|---|---|
| V1 | **Fiamma torcia visibile** | Points/sprite + noise shader TSL, 50-100 particelle additive | La torcia deve avere un fuoco visibile (oggi solo SpotLight) | ✅ `handFlame`/`placedFlame` in ThreeRendererService (group+update+intensity) |
| V2 | **Trail arma** | Mesh ribbon aggiornata per frame durante ACTIVE | VFX `vfx_slash_trail` già dichiarato | ✅ `weaponTrail` (slash+update) in ThreeRendererService; wired in GameApplication |
| V3 | **Polvere scavo** | THREE.Points burst a ogni SEGMENT_COMPLETE | Feedback scavo | ✅ `sparkBurst`/`emitSparks()` — wired a ogni dig event in GameApplication |
| V4 | **Dissolve nemico** | Shader TSL threshold + noise | Morte nemici al posto del pop | ✅ `createDissolveMaterial()` su `enemyMaterial`, animato in `setEnemyStates` |
| V5 | **Bloom/vignette/grain** | pmndrs/postprocessing (o three/examples) | Look Noir | ✅ BloomNode TSL (WebGPU) + UnrealBloomPass (WebGL2); verificato in browser |
| V6 | **God rays volumetrici** | ✅ **Fake volumetric (cross-backend)** | Cono additivo trasparente che segue la camera a torcia accesa (opacity pulsante col flicker) + god ray sul reliquiario del tesoro (raggio ascendente + bagliore) — funziona su WebGL2 E WebGPU senza ray-marching TSL. `torchBeam` in ThreeRendererService (creato in init, animato nel render loop, nascosto a torcia spenta); beam+glow nel `setLootReliquary`. Momenti narrativi coperti (tesoro raro). |
| V7 | **Screen shake** | ✅ già implementato (addCameraShake + reduceCameraShake) | Danno al player | ✅ fatto |

### 1.7 Tool / Pipeline (G-17, G-21)

| # | Tool | URL | Uso | Stato |
|---|---|---|---|---|
| P1 | **GLTFLoader + DRACOLoader** | three/examples/jsm/loaders/ (in three 0.185) | Caricamento .glb — zero nuove dipendenze | ⬜ AssetLoader da creare |
| P2 | **gltf-transform** | gltf-transform.dev | Ottimizzazione: Draco/meshopt, quantizzazione, dedup — script CI | ✅ `scripts/optimize-assets.mjs`; 12/12 GLB ottimizzati; PWA 15.9→8.8MB |
| P3 | **Workflow CI** | .github/workflows/verify.yml | ✅ già pronto (verify + e2e + PROPERTY_RUNS) | ✅ fatto |
| P4 | **F3 debug overlay** | `src/ui/DebugOverlay.ts` | Draw calls, ms/sistema, entità | ✅ implementato (`DebugOverlay.ts`, test DOM) |

---

## 2. PIANO DI INTEGRAZIONE (ordine di esecuzione)

> Ogni passo = commit logico. Al termine di ogni passo: `npm run verify` verde.

| Passo | Gap | Azione | Output | Stato |
|---|---|---|---|---|
| 1 | G-17 | **AssetLoader + manifest** `content/assets.ts` (mappa archetipo/landmark → URL .glb) + cache Map | src/rendering/AssetLoader.ts + src/content/assets.ts + test | ✅ fatto (2026-08-13) |
| 2 | G-14 | **Ambiente modulare**: integrare Quaternius Ruins / ToxSam GLB nel ThreeDungeonLayout (le 9 famiglie di G-23 diventano punti di aggancio) | ThreeDungeonLayout upgrade | ✅ **completo 5/5 landmark GLB** (ToxSam CC0 + Poly Pizza) |
| 3 | G-16 | **Materiali custom**: geroglifici emissivi (T3) + dissolve MUMMY (V4) + sabbia ambientCG (T1) | shader TSL/onBeforeCompile | ✅ fatto (2026-08-13) + geroglifici emissivi sui landmark glyph (2026-08-14) |
| 4 | G-13 | **Nemici**: SCARAB (A6), MUMMY (dissolve + rig), poi gli altri archetipi per tier | modelli + hurtbox visuali | ✅ **completo 7/7 GLB** (runtime data-driven + modelli ToxSam/Poly Pizza) |
| 5 | G-15 | **VFX**: fiamma torcia (V1) → trail (V2) → polvere scavo (V3) → bloom/vignette (V5) | effetti in ThreeRendererService | ✅ **completo** (fiamma, scintille, trail falce, polvere, vignette/grana/respiro DOM, bloom UnrealBloomPass su WebGL2 gated + try/catch, contrasto alto ⇒ bloom ridotto) |
| 6 | G-18/19 | **Audio**: Kenney SFX + Sonniss foley + ambience; AudioAssetLoader con preload; wiring cue arma/combat in AudioEventDirector | audio reale | ✅ 17 OGG Kenney + 13 RPG foley; `WebAudioEngine.play()` ramo reale/proc; drone 4-layer (B-05); musica adattiva 3 stati |
| 7 | G-12 | **ART_BIBLE.md**: palette finale, silhouette, regole telegrafi, moodboard | docs/ART_BIBLE.md | ✅ fatto (2026-08-13) |
| 8 | G-03 | **HurtboxSystem generale** + spawn MUMMY materializzabile | systems + test | ✅ fatto (2026-08-13) |
| 9 | G-05 | **Graft equipaggiabili** (UpgradeSystem → slot arma reali) | gameplay + test | ✅ fatto (2026-08-13) |
| 10 | G-22 | **E2E run-completion + seed variance** | e2e | ✅ fatto (2026-08-14): 11/11 verdi — riproducibilità ?seed=42, varianza seed 42 vs 777, fallback WebGL2 |

---

## 3. REGISTRO IMPLEMENTAZIONE (aggiornato ad ogni passo)

| Data | Passo/Gap | Esito | Dettagli |
|---|---|---|---|
| 2026-08-16 | **Tutorial graduale (G-22 esteso)** | ✅ **11 hint contestuali cablati** | `src/ui/HUD.ts`: `ContextualHint` + coda `pendingHints` + dedupe per id (`shownHintIds` — ogni hint appare una sola volta per run) + `ensureHintEl()` lazy (zero costo se non usato) + fade in/out 300ms + `aria-live=polite`. Elemento `#contextual-hint` in basso al centro, palette papiro. Wiring in `GameApplication` nei momenti insegnabili: `hint-relic` (reliquiario → TAB), `hint-gold` (primo oro → conversione in Frammenti), `hint-lowhp` (HP ≤30% → para/schiva), `hint-damage` (primo danno subito → telegrafi + schivata), `hint-fuel-low` (torcia quasi spenta → braciere E), `hint-brazier` (braciere acceso → oscurità arretra), `hint-curse` (maledizione deposta → trade-off + Sangue di Ra), `hint-enemy` (primo spawn nemico → click sx, schiva Spazio), `hint-stair` (scala → piani più profondi), `hint-dig` (scavo → E per scavare), `hint-dodge` (prima schivata), `hint-parry` (parata riuscita → stordimento + click sx). Dispose completo (timer, coda, set). Test: 4 in `tests/dom/HudHints.test.ts` (dedupe per id, coda FIFO, fade, lazy creation). `npm run verify` verde. |
| 2026-08-13 | — | 📄 Roadmap creata | Documento di riferimento per l'integrazione professionale. Risorse verificate online. |
| 2026-08-13 | G-12→G-19 | ✅ Documento risorse | `docs/RISORSE_PROFESSIONALI.md` — guida completa con URL verificati, licenze e uso. |
| 2026-08-13 | **Passo 1 — G-17** | ✅ **AssetLoader + manifest** | `src/rendering/AssetLoader.ts` (GLTFLoader con cache Map, fallback silenzioso a null, preload, clear) + `src/content/assets.ts` (manifest 8 archetipi + 18 landmark, `validateAssetManifest()` come invariante, `enemyAssetFor`/`landmarkAssetFor`). Preload fire-and-forget in `ThreeRendererService.init()` — non blocca il bootstrap, fallback alle primitive G-23 se gli asset mancano. Test: 6 in `tests/content/assets.test.ts` (node) + 4 in `tests/dom/AssetLoader.test.ts` (cache/fallback/preload/clear, fetch mockato a 404). `npm run verify` verde. |
| 2026-08-13 | **Passo 3 — G-16** | ✅ **Materiali custom procedurali** | Nuovo `src/rendering/Materials.ts`: `createHieroglyphTexture()` (pattern geroglifici stilizzati — ankh, occhio di Horus, tratti — deterministico, CanvasTexture con RepeatWrapping), `createSandTexture()` (granuli deterministici via LCG hash, repeat 6×6), `createDissolveMaterial()` (MeshStandardMaterial + `onBeforeCompile` che inietta clipping per soglia con bordo emissivo dorato e noise). Integrati in `ThreeRendererService`: texture sabbia sul pavimento (fallback materiale piatto se canvas assente), materiale nemico dissolve guidato da `setEnemyDissolve` in `setEnemyStates` (1 quando un nemico muore, decadimento a 0). Test: 4 in `tests/dom/Materials.test.ts` (mock canvas 2D via `document.createElement` spy — happy-dom non implementa il context). `npm run verify` verde. |
| 2026-08-13 | **Passo 2 — G-14** | ✅ **Infrastruttura ambiente modulare** | `public/assets/README.md` con struttura attesa e istruzioni di download (Quaternius Ruins CC0, ToxSam GLB registry, Mixamo). Integrazione runtime: `loadLandmarkModels()` in `ThreeRendererService` — dopo `buildDungeonLayout`, carica i .glb dei landmark dal manifest e li posiziona sopra la primitiva placeholder (scale/yOffset dal manifest, shadow abilitate). Se il modello manca → fallback primitiva. Quando i .glb reali entrano in `public/assets/`, vengono istanziati automaticamente senza toccare codice. |
| 2026-08-13 | **Passo 5 — G-15 (V1+V3)** | 🟡 **Fiamma torcia + scintille** | Nuovo `src/rendering/Vfx.ts`: `createTorchFlame()` (fiamma procedurale: cono emissivo arancio + nucleo bianco-oro + PointLight calda, flicker a 3 frequenze, `setFlickerReduced` per accessibilità) e `createParticleBurst()` (pool 128 particelle additive, buffer riusati, gravità, fade, `emit`/`update`/`idle`). Integrati in `ThreeRendererService`: fiamma in mano agganciata alla camera (viewmodel), fiamma sulla torcia posata, `emitSparks()` nell'handle (collegate al colpo del player in `GameApplication.processInput`). Test: 4 in `tests/dom/Vfx.test.ts`. Residui G-15: trail arma (V2), bloom/vignette (V5), polvere scavo (V3-parziale). |
| 2026-08-13 | **Passo 6 — G-18 (parziale)** | 🟡 **Cue di combattimento + wiring** | `ProceduralCueLibrary` estesa con 4 profili sintetici: `attack_swing` (sawtooth 620→340 Hz, fendente), `attack_hit` (square 190→90 Hz, impatto), `player_dodge` (triangle 500→760 Hz, schivata), `gold_pickup` (sine 880→1320 Hz, monete). Wiring in `GameApplication`: swing sul colpo del player, dodge sulla schivata, gold sull'aggiunta monete (con HUD "+N monete"). Test: 3 in `tests/audio/ProceduralCueLibrary.test.ts` (profili validi, firme distinguibili, fallback default). Residui G-18/19: sample reali (Kenney/Sonniss), ambience per i livelli di oscurità, preload AudioAssetLoader. |
| 2026-08-13 | **Passo 7 — G-12** | ✅ **ART_BIBLE.md** | Nuovo `docs/ART_BIBLE.md`: identità "Egyptian Noir", palette canonica (8 colori con esadecimale e uso), linguaggio delle forme (6 famiglie di silhouette — vincolo: nessun nemico condivide la silhouette di un landmark critico), semantica del colore per ruolo (emissivo rosso=pericolo attivo, turchese=conoscenza, oro=ricompensa), illuminazione a 4 livelli (la torcia è il personaggio), telegrafi per nemico (intento→finestra→controgio), materiali implementati in G-16, UI papiro, audio come arte, checklist di qualità visiva in 10 punti e referenze per i passi futuri. Contratto design↔rendering per gli asset in arrivo. |
| 2026-08-13 | **Passo 8 — G-03** | ✅ **Hurtbox + MUMMY materializzabile** | Nuovo `src/gameplay/combat/EnemyAttackResolver.ts`: hit detection unificata nemico→giocatore (riusa `collectAttackHits` con hurtbox virtuale del player a capsula — parametri da `PLAYER.capsuleHeightM/RadiusM`; `createPlayerHurtbox()`, `resolveEnemyAttackHitsPlayer()`). Nuovo `src/gameplay/enemies/MummyEncounterRuntime.ts`: encounter completo — wake 2.5s con LOS, pursuit lenta (rotazione 60°/s), fendente ARC 120° con telegrafo 1.0s e danno risolto al primo tick ACTIVE (hit-once via HitRegistry), recoil alla luce (torchAffinity −0.6), `getMummyTelegraphStrength()` per il renderer. Wiring in `GameApplication`: `mummyState` (tick nel loop, sync ECS, hurtbox nel store, danno ramo in `resolvePlayerAttack`, follow-up del Director che ora materializza MUMMY oltre a SCARAB con slot indipendenti, reset a fine piano). Test: 5 (`EnemyAttackResolver.test.ts`) + 7 (`MummyEncounterRuntime.test.ts`). `npm run verify` verde. |
| 2026-08-14 | **Passo 9 — G-05** | ✅ **Graft equipaggiabili** | Nuovo `src/gameplay/upgrades/UpgradeResolver.ts`: `resolveCombatModifiers()` (aggregazione dei `StatModifier` degli innesti in un `CombatModifiers` unico — MULTIPLY si compone, ADD si somma, clamp anti-negativi), `upgradesFromNames()` (mappa i nomi scoperti nel profilo alle definizioni, ignora gli sconosciuti), `resolvePlayerDamage()` (danno finale: damageMultiplier, bonus undead, crit da dietro vs frontale). Wiring in `GameApplication`: `combatModifiers` derivato in `syncProgressionRuntimeBonuses` dai `discoveredGrafts` del profilo (catalogo `ALL_UPGRADES`); `resolveVerticalSliceAttack` ora usa `resolvePlayerDamage` con `targetIsUndead` (MUMMY guardiana e mummia materializzata sì, SCARAB no). Test: 6 in `tests/gameplay/upgrades/UpgradeResolver.test.ts` (neutro, bronzo, composizione, lapis undead, dedup, clamp). `npm run verify` verde con **268 test node + 20 DOM = 288 test**, build ✓. Verifica browser: zero errori JS. |
| 2026-08-14 | **Grafica — geroglifici emissivi + vignette + trail + polvere** | ✅ **G-15 V2/V3/V5-parziale + G-16 esteso** | **(1)** Geroglifici emissivi: `ThreeDungeonLayout` accetta `glyphEmissiveMap` opzionale — i landmark di tipo glyph ora usano la `CanvasTexture` geroglifica procedurale come emissiveMap turchese (`#6EE0D1`, intensità 0.9), creata in `ThreeRendererService.init()` e condivisa. **(2)** Nuovo `src/ui/CinematicOverlay.ts`: vignette radiale + grana (pattern CSS overlay) + respiro del buio (opacità pilotata da `setDarknessBreath`) + bordo porpora a HP ≤ 30% (`setLowHp`) — DOM puro, pointer-events none, z-index 12, dispose. Wiring in GameApplication: respiro = darkness accumulata × 0.45 (1.0 a torcia spenta), low-HP a ≤30%, dispose nel teardown. **(3)** Trail arma: `createWeaponTrail()` in Vfx.ts (torus 180° emissivo additivo, vita 220ms) + `playWeaponTrail(pos, yaw)` nell'handle, collegato al colpo del player. **(4)** Polvere scavo: `emitSparks` colore sabbia `#8A7350` su ogni `DIG_PROGRESS`. Test: +2 trail (Vfx.test.ts) +4 overlay (CinematicOverlay.test.ts). `npm run verify` verde: 268 node + 26 DOM = **294 test**. |
| 2026-08-14 | **Bloom selettivo (G-15 V5 completo)** | ✅ **G-15 chiuso** | `ThreeRendererService`: su backend `webgl2` crea `EffectComposer` + `RenderPass` + `UnrealBloomPass` (strength 0.55, radius 0.55, threshold 0.42) da `three/examples/jsm/postprocessing` (zero dipendenze nuove) — import dinamici, try/catch con fallback al rendering diretto; `composer.render()` nel loop quando attivo; `composer.setSize` nel resize; `dispose` nel teardown; contrasto alto (accessibilità) ⇒ strength 0.22. WebGPU mantiene il rendering diretto (il suo pipeline non usa EffectComposer). Nuovo e2e "fallback WebGL2: il gioco parte senza WebGPU" (blocca `navigator.gpu` → il gioco degrada, menu visibile, discesa avviabile). **9/9 e2e verdi**, verify verde (268 node + 26 DOM). G-15 ora completo: V1 fiamma, V2 trail, V3 polvere, V4 dissolve, V5 vignette+grana+bloom. |
| 2026-08-14 | **Drone ambientale procedurale (G-18)** | ✅ **Audio del buio implementato** | Nuovo `src/audio/AmbiencePreset.ts` (puro): `AMBIENCE_PRESET` — 3 layer (drone grave 55Hz La1 + quinta 82.4Hz Mi2, ciascuno con 2 oscillatori detuned per battimento organico, + shimmer triangle 330Hz), LFO di tensione 0.11Hz con depth 0.35, `ambienceGainForDarkness()` (curva quadratica: silenzioGain 0.02 → 0.87 al buio pieno), `lfoDepthForDarkness()`. `WebAudioEngine`: `setAmbienceLevel(darkness01)` — costruisce il drone all'unlock (bus ambience), transizioni morbide 1.5s via `linearRampToValueAtTime`, LFO modula i gain, dispose del drone nel teardown. Wiring in `GameApplication`: `audio.setAmbienceLevel()` nello stesso punto del respiro visivo (torcia spenta ⇒ 1.0, altrimenti darkness × 0.55 clamp 0.45). Test: 4 in `tests/audio/AmbiencePreset.test.ts` (preset valido, curva quadratica, clamp, LFO depth). `npm run verify` verde (272 node + 26 DOM = **298 test**), browser zero errori JS. |
| 2026-08-14 | **Download asset 3D completo (G-13/G-14) — 12/12** | ✅ **Tutti i modelli presenti** | Seconda ondata da **Poly Pizza** (`poly.pizza/search/<term>` → pagina modello → `static.poly.pizza/<uuid>.glb`): `scarab.glb` = Beetle (64KB), `mummy.glb` = Zombie (681KB, bendato umanoide), `shabti.glb` = Colossus pre Tilt Brush (580KB, guardiano di pietra), `sobek.glb` = Crocodile (38KB), `royal_mummy.glb` = Skeleton (485KB, CC0). Licenze: 4 CC-BY (Beetle/Zombie/Crocodile "Poly by Google", Colossus "Darwin Yamamoto") + 1 CC0 (Skeleton) — attribuzione documentata nel README asset. **Verifica browser: `Asset preload completato {12/12}`, ZERO 404, zero errori JS** (prima: 12 warning). G-13 e G-14 ora completi. |
| 2026-08-14 | **GenericEncounterRuntime (G-13 runtime + G-03 residuo)** | ✅ **Tutti gli archetipi materializzabili** | Nuovo `src/gameplay/enemies/GenericEncounterRuntime.ts`: runtime data-driven che legge `EnemyDef` da content/enemies per QUALSIASI archetipo combattibile — FSM DORMANT→PURSUING→ATTACKING (hit-once via HitRegistry), wake con hearRadius o LOS, selezione attacco per distanza, telegrafo crescente durante l'anticipazione, torchAffinity (±: la luce attrae lo Scarabeo, respinge Mummie), arretra se la luce respinge a distanza <1.2m, `attackDefinitionFromDef` (ARC o SPHERE per arcDeg≥340, parryable = !isHeavy). **2 bug trovati e fixati**: (1) i nemici camminavano OLTRE il player senza mai allinearsi — ora si fermano in range e chiudono l'angolo; (2) convenzione angolare — `withinArc` usa `atan2(-dx,-dz)` (yaw 0 = -z), il runtime usava `atan2(dx,dz)` → ARC mancava sempre. Wiring in `GameApplication`: slot `genericEnemyState` — tick nel loop (danno → applyDamageToPlayer), sync ECS con hurtbox proporzionale (COBRA piccolo, altri 1.75m), danno nel ramo resolvePlayerAttack (undead = MUMMY/ROYAL_MUMMY per il bonus Lapislazzuli), sync visivo enemyStates, follow-up del Director ora materializza QUALSIASI archetipo. Test: 7 in `tests/gameplay/enemies/GenericEncounterRuntime.test.ts` (wake da suono, hit-once con gap≥100 tick, selezione attacco SHABTI, telegrafo ROYAL, recoil luce, morte, shape SPHERE/ARC). Verify verde: **283 node + 26 DOM = 309 test**, 11/11 e2e. |
| 2026-08-14 | **Passo 10 — G-22 (seed variance + fallback)** | ✅ **Roadmap 10/10** | `parseSeedParam()` in `src/app/createGame.ts` (export puro, testato): `?seed=N` o `?fixedSeed=N` nell'URL → `config.debug.fixedSeed` (merge con DEFAULT_CONFIG per il Partial di Zod) → generazione riproducibile. 3 nuovi e2e: **riproducibilità** (2 run con ?seed=42 → stessa fingerprint SVG della minimappa), **varianza** (seed 42 vs 777 → fingerprint diverse), **fallback WebGL2** (blocca navigator.gpu → gioco degrada, menu visibile, discesa avviabile). Fix race: i test seed girano in serie (`test.describe.configure({ mode: 'serial' })`) perché condividono l'origin/IndexedDB del profilo. Test: 4 unit (`tests/app/createGame.test.ts`). **11/11 e2e verdi**. Con questo, tutti i 10 passi della roadmap sono completati o infrastruttura-pronti. |
| 2026-08-14 | **Batch report v2 (run summary + debug overlay + hitmarker differenziale + fade piani + decor fascia + geroglifici generativi)** | ✅ **6/6 implementati e verificati** | Dal report `docs/ANALISI_DEEP_MIGLIORAMENTI_v2.html` (scorecard 7.9/10, gap per ROI×sforzo — QualityController già cablato nel batch precedente): **(1) Run summary**: `DeathOverlay` estesa con piani percorsi/nemici abbattuti/oro raccolto (emoji 🏺⚔️🪙) — tracker `runStats` in GameApplication (floorsCleared alla discesa, enemiesDefeated nei 4 rami di morte, goldEarned alla morte). **(2) Debug overlay F3/Backquote**: nuovo `src/ui/DebugOverlay.ts` — FPS, frame ms, draw calls, triangoli, memoria, entity count, floor seed hex, tier qualità, versione, backend; `RendererService.getDebugStats()` (renderer.info), `World.entityCount` + `EntityAllocator.aliveCount` (tracciato incrementale); update solo se visibile (zero costo). **(3) Hitmarker differenziale**: `showHitmarker(kind)` — oro=colpo, rosso=critico (`wasCritical` catturato), grigio=mancato. **(4) Dissolvenza nera tra piani**: `CinematicOverlay.fadeToBlack()` — fade 380ms attorno al rebuild in `descendToNextFloor` (maschera il loading stutter). **(5) Decor per fascia**: `FloorModel.floorIndex`/`FloorSceneLayout.floorIndex` (nuovi) + `DecorateRoomsOptions.mood` — Cripta (piani 5+): pool ossa/scheletri/rug; Anticamera (1-2): vasi/colonne; Galleria: neutro. **(6) Geroglifici generativi per seed**: `src/content/inscriptions.ts` — `generateInscription(seed)` hash32 → 12-20 glifi Noto Egyptian + preambolo "Sotto <sigillo>, la piramide sussurra…" mostrati nel MainMenu (?seed=N o ?daily). Test: +3 DebugOverlay DOM, +1 hitmarker differenziale, +4 inscriptions node; fixture aggiornate (floorIndex). **Verify verde: 314 node + 34 DOM = 348 test**, 11/11 e2e, build ✓. Browser: iscrizione geroglifica confermata (16 glifi, seed 42). |
| 2026-08-14 | **Doorway snap assist — niente più incastri sulle porte (G-18 V4)** | ✅ **Movimento fluido attraverso gli stipiti** | Il player non si incastra più sui bordi delle porte: `FloorSceneLayout` ora espone `doorways` (centro di ogni apertura = punto medio del corridoio, con asse x/z). `PlayerCharacterController.applyDoorwaySnapAssist()` (funzione pura esportata, testabile): quando il player è entro 1.6m da una porta e il movimento punta verso l'apertura (dot > 0.15), la direzione viene deviata dolcemente verso il centro (intensità 0.55 × prossimità × allineamento, poi normalizzata) — la capsula scorre dentro il passaggio invece di fermarsi sullo spigolo. Wiring: `playerController.setDoorways()` all'init e in `descendToNextFloor()` (nuovo piano). Test: **6 in `tests/gameplay/player/DoorwaySnapAssist.test.ts`** (nessuna porta, lontano, deviazione verso centro, più vicino = più forte, direzione opposta invariata, allineato = nessuna deviazione). **Verify verde: 300 node + 30 DOM = 330 test**, 11/11 e2e. |
| 2026-08-14 | **Slider mouse look percentuali nel menu impostazioni (G-18 V3)** | ✅ **Sensibilità e smoothing regolabili in %** | `SettingsMenu` sezione Controlli: **Sensibilità Mouse** (0.1–5.0 = 10–500%) e **Smoothing Mouse** (0–95%) — entrambi con display percentuale live (`sliderRow` accetta `format` callback; `populateForm` mostra `N%` per mouseSensitivity/mouseSmoothing). Flusso completo: `RuntimeSettings` esteso con `mouseSmoothing` → `buildRuntimeSettings`/`applyRuntimeSettings`/`cloneRuntimeSettings` (SettingsMenu + Persistence) → `RuntimeSettingsSchema` con default 0.55 (**retrocompatibile**: i vecchi save senza il campo ricevono il default). `readSavedRuntimeSettings`/`writeRuntimeSettingsToSave` aggiornati. Test: +2 DOM SettingsMenuMouse (slider % iniziale, % live su input) +1 node retrocompatibilità (save legacy → default). **Verify verde: 294 node + 30 DOM = 324 test**, 11/11 e2e. |
| 2026-08-14 | **Luce torcia realistica + mouse smoothing + intro cinematografica (G-18 V3)** | ✅ **Feedback gameplay professionale** | **(1) Luce torcia**: SpotLight da 36°→**56°** (`Math.PI/3.2`), distanza 35→45m, penombra 0.4→0.85, colore più caldo `#FFB45E` + nuovo `torchAmbientLight` (PointLight alone caldo 9m che segue la camera) — flicker arricchito a 5 sinusoidi (respiro 0.23Hz + tremolio + sfarfallio 11.3Hz + deriva vento 0.13Hz). **(2) Mouse smoothing**: `controls.mouseSmoothing` (Zod, default 0.55) — filtro esponenziale su yaw/pitch (`lookYawFiltered += (raw - filtered) * alpha`), niente più scatti da delta grezzi. **(3) Intro cinematografica**: alla discesa la camera panoramica la stanza d'ingresso (posa rialzata 2.3m + pan sinusoidale, 3.2s) mostrando la **torcia posata da raccogliere** (E) — la raccolta accende la torcia (TOGGLE), rimuove la posata, e SOLO ALLORA appare il tutorial dei comandi. E2E aggiornati al nuovo flusso (intro → KeyE → tutorial). Test: +2 HudJuice DOM (hitmarker, crosshair staticCrosshair). **Verify verde: 294 node + 28 DOM = 322 test**, 11/11 e2e. |
| 2026-08-14 | **Meta-loop multi-floor G-10 (design + runtime + juice)** | ✅ **Discesa nella piramide implementata** | `docs/DESIGN_LIVELLI_SUPERIORI.md` (design 10 piani: temi per fascia, scale a chiocciola, difficoltà per composizione — mai HP) + `src/content/floorProgression.ts` (10 piani: palette, budget Director 12→80, darknesFactor crescente, `floorSeed()` deterministico) + `exitIsStair` nel FloorModel (piani 1-9 scala, piano 10 porta) + LandmarkPlaceholders STAIR (pozzo turchese) + risoluzione `'STAIR'` in tryCompleteSlice + `descendToNextFloor()` (rigenera slice con seed derivato, ricrea Director/guardiana/bracieri/dig site, `playerController.teleport()`) + `applyFloorPalette()` nel renderer (FogExp2 crescente, colori per fascia) + budget Director da floorProgression. **50 miglioramenti juice/accessibilità**: 8 cue audio nuovi (footstep_sand con rate-limit 450ms + dust puffs, stair_descend, door_creak, ui_click menu, fragment_pickup, player_death, attack_hit con camera shake + hitmarker X 90ms, parry_success) + 2 opzioni accessibilità nuove (reduceParticleEffects, staticCrosshair con crosshair DOM) + HUD hitmarker/crosshair + test invarianti Director (template per piano, budget ≥ maxCost). Test: 5 floorProgression + 3 FloorProgressionDirector. **Verify verde: 294 node + 26 DOM = 320 test**, 11/11 e2e, browser zero errori. |

| 2026-08-16 | **Parry system (gap reale)** | ✅ **Parata completa** | Nuovo `src/gameplay/combat/ParryResolver.ts` (puro): `PARRY_WINDOW_MS 350`, `PARRY_IFRAME_MS 300`, `PARRY_STAGGER_TICKS 48` (0.8s), `PARRY_ARC_DEG 100`, `parryWindowActive()`/`angleToEnemyDeg()`/`isEnemyInParryArc()` — convenzione angolare IDENTICA al resolver (`atan2(-dx,-dz)`, yaw 0 = -z). `GenericEncounterRuntime`: nuovo stato FSM `STAGGERED` + `staggerTicks`, check parata al primo tick ACTIVE (parryable = !isHeavy), opzione `parryWindowActive` nei tick options, risultato `parried`. `MummyEncounterRuntime`: idem (MUMMY_SLASH parryable) con stordimento via `staggerTicks`. Wiring `GameApplication`: handler `ActionKind.Parry` (tasto destro) → finestra + i-frame + whoosh; i-frame di parata in `applyDamageToPlayer` (protegge dagli altri nemici); `parry_success` + camera shake + hitmarker + messaggio + hint. **Test: 6 ParryResolver + 3 generic + 2 mummy = 11 nuovi** (finestra, arco, simmetria, stordimento, pesanti non parabili, dietro non parabile). Verify verde. |
| 2026-08-16 | **Musica adattiva G-19** | ✅ **3 stati crossfadati** | Nuovo `src/audio/MusicPreset.ts` (puro): `MUSIC_PRESET` EXPLORE (drone La2/Mi3 + respiro 0.12Hz) / TENSION (saw La1/Mi2 + pulsazione 2.2Hz) / COMBAT (battimento + square 6Hz + shimmer 9Hz), masterGain crescente, lowpass per stato, `validateMusicPreset()`. `WebAudioEngine.setMusicState()`: crossfade 1.5s (ramp out vecchi layer + stop ritardato, build nuovi con tremolo LFO per layer), bus music pilotato, `buildMusic` all'unlock, dispose. Wiring `GameApplication.updateMusicState()` (una volta per frame): COMBAT se un nemico è ATTACKING/CHARGING, TENSION se qualcuno è sveglio, EXPLORE altrimenti. Test: 5 (`MusicPreset.test.ts`). Verify verde. |
| 2026-08-16 | **Viewmodel arma 3D (gap reale)** | ✅ **Khopesh procedurale** | Nuovo `src/rendering/WeaponViewmodel.ts`: khopesh interamente procedurale (lama a falce TubeGeometry lungo CatmullRomCurve3, filo dorato emissivo, impugnatura, guardia, pomolo; bronzo metalness 0.9) agganciato alla camera (posa (0.38,-0.36,-0.62)); `playSwing()` (fendente 180ms easeOutCubic su Z -1.15→0.5) e `playParry()` (guardia 260ms easeOutBack su X→-1.3) con ritorno elastico alla posa; tempo interno (`update(deltaMs)`) per testabilità. RendererHandle: `playWeaponSwing()`/`playWeaponParry()`/`setWeaponViewmodelVisible()`; wiring in GameApplication (attacco, parata, cambio arma slot 1/3 nasconde, slot 2 khopesh mostra). Test: 5 DOM (`WeaponViewmodel.test.ts`). Verify verde. |
| 2026-08-16 | **HDRI/env map procedurale** | ✅ **Riflessioni metalli (WebGL2)** | `ThreeRendererService.init()`: `PMREMGenerator.fromScene` su scena ambiente scura Egyptian Noir (PointLight calda 0xffb45e + turchese 0x2e8b8b + Directional oro), `scene.environment` + `environmentIntensity 0.55`, `envMapTexture` con dispose. I metalli (bronzo, oro, khopesh) riflettono accenti ambrati/turchesi. WebGPU: limitazione documentata (il suo pipeline TSL non usa PMREM). |
| 2026-08-16 | **Texture PBR ambientCG (gap reale)** | ✅ **Ground054 + Rock064 complete** | Download ambientCG CC0 via API v2 (`ambientcg.com/api/v2/full_json?include=downloadData` → `get?file=<id>_1K-JPG.zip`): **Ground054** (sabbia) e **Rock064** (pietra), mappe Color/NormalGL/Roughness/AO estratte e ricompresse (PIL q82, 1024px) in `public/textures/` (8 file, ~1.6MB totali). `loadPbrTextureSet()` esteso con roughnessPath/aoPath; floor (sand_*, repeat 6×6) e muri (stone_*, 4×3) cablati con `roughnessMap` + `aoMap` (aoMapIntensity 0.7/0.75); `ThreeDungeonLayout.addDungeonBox` ora aggiunge `uv2 = uv` (requisito aoMap); fallback procedurale invariato. `CREDITS.md` aggiornato (attribuzione corretta Ground054/Rock064). |
| 2026-08-16 | **Kenney SFX reali (gap reale)** | ✅ **Asset audio CC0 cablati** | Download Kenney pack Impact + Interface (pattern `kenney.nl/media/pages/assets/<pack>/<hash>/kenney_<pack>.zip`): 17 OGG selezionati in `public/audio/` (~150KB): footstep×4 (grass≈sand), hit×3, crit×3, parry×3 (metal clang), click×2, confirmation×2. Nuovo `src/audio/AudioAssetLibrary.ts` (puro): `AUDIO_ASSET_MAP` cue→varianti + `validateAudioAssetMap()`. `WebAudioEngine.loadAssets()` (fetch+decodeAudioData, fallback silenzioso) + branch in `play()`: se il cue ha buffer reali usa variante casuale (volume/rate/panner/loop), altrimenti profilo procedurale. Wiring: `unlockAudio()` in GameApplication chiama `loadAssets()` dopo l'unlock. Test: 3 (`AudioAssetLibrary.test.ts`). `CREDITS.md` sezione Audio. |
| 2026-08-16 | **gltf-transform + Meshopt (gap reale)** | ✅ **12/12 GLB ottimizzati, PWA 15.9→8.8MB** | `@gltf-transform/cli@^4.4.2` (devDep) + `scripts/optimize-assets.mjs` (`node scripts/optimize-assets.mjs [--draco]`): `optimize --texture-compress false` su tutti i GLB (dedup/prune/quantize+meshopt) → **-1.078KB**; entry JS diretta (`node_modules/@gltf-transform/cli/bin/cli.js`, niente npx/.cmd su Windows). ⚠️ **PITFALL CRITICO**: i GLB compressi richiedono `GLTFLoader.setMeshoptDecoder(MeshoptDecoder)` (`three/examples/jsm/libs/meshopt_decoder.module.js` — wasm incorporato, Vite lo bundla) — senza, il loader rifiuta i file (`setMeshoptDecoder must be called`) e i modelli cadono sulle primitive. Verificato in browser: 12/12 caricati, zero warning. |
| 2026-08-16 | **InstancedMesh decor (gap reale)** | ✅ **Una draw call per tipo** | `RoomDecor.ts` refactored: passo 1 raccoglie i posizionamenti deterministici (stessa logica hash32), passo 2 costruisce UN `InstancedMesh` per DecorType (matrici via Object3D dummy, rotation sdraiata per skeleton/rug, castShadow per tipo). Draw call decor: N → ≤8. Test RoomDecor aggiornati per leggere le posizioni dalle `instanceMatrix` (3/3 verdi). |
| 2026-08-16 | **Bloom WebGPU (gap reale)** | ✅ **RenderPipeline + BloomNode TSL** | `ThreeRendererService`: su backend webgpu crea `RenderPipeline` (r183+, da `three/webgpu`) con `pass(scene, camera)` + `bloom(color, 0.55, 0.55, 0.42)` (`three/addons/tsl/display/BloomNode.js`) → `outputNode = color.add(bloom)`, render nel loop, dispose. Fallback try/catch al rendering diretto. **Verificato in browser WebGPU: "Bloom WebGPU attivo", zero errori JS** (insieme a: 12/12 asset meshopt, env/SSAO/quality invariati). |
| 2026-08-17 | **Verifica A-01/A-02 — gap chiusi** | ✅ **Confermati chiusi da audit** | **A-01** (`noiseStimulus`): verificato in `GenericEncounterRuntime.tick()` — campo `noiseStimulus` presente, `canHearNoise` logica nel ramo DORMANT. **A-02** (`amplifiedTelegraphs`): verificato in `ThreeRendererService.setEnemyStates()` — `const telegraphGain = presentation.amplifiedTelegraphs ? 1.9 : 1.0` applicato a `mesh.scale` e `material.emissiveIntensity`; `applyPresentation()` scrive `presentation = settings`. Entrambi i gap confermati chiusi senza modifiche al codice. |
| 2026-08-17 | **Calibrazione nemici (A-03)** | ✅ **COBRA + SHABTI ribilanciati** | `src/content/enemies.ts`: **COBRA** `anticipationTicks` 0.25→0.30s (15→18 tick; parata a 350ms ora più leggibile) + `hearRadiusM` 12→10 (ridotto raggio udito ambush). **SHABTI** `baseHp` 120→100 (~6 colpi khopesh, da 7) + `Carica di Pietra` `range` 8.0→6.5m (da quasi-room-width a gestibile). Motivazione: DPS analysis COBRA≈3.2 HP/s, SHABTI≈6 HP/s; carica a 8m lasciava zero spazio di fuga in stanza 12m. |
| 2026-08-17 | **Ambience drone 4° layer (B-05)** | ✅ **Rombo di pietra + tuning per floor** | `src/audio/AmbiencePreset.ts`: aggiunto layer `{ waveform:'sawtooth', frequencyHz:110, detuneCent:-10, gainMax:0.08 }` (La2 metallico, risonanza corridoi). Nuove funzioni pure esportate: `gainMultiplierForFloor(floor)` → ×1.0..×1.4 (piano 1→10, lineare, clamp); `stoneRumbleGainForFloor(floor, gainMax)` → 0 ai piani 1-2, sale linearmente al piano 7 (piena ampiezza). Test aggiornati in `AmbiencePreset.test.ts`: "4 layer" + 2 test B-05 (range moltiplicatore, assenza piani bassi). |
| 2026-08-17 | **B-04-ext: cue sintetici nemici (ProceduralCueLibrary)** | ✅ **11 profili attacco + 4 test** | `src/audio/ProceduralCueLibrary.ts`: 11 nuovi casi in `buildCueNode()` — `scarab_click` (square 1200→700Hz, 0.04s — scatto breve), `snake_hiss` (noise lowpass 5500Hz, 0.55s — sibilante), `mummy_creak` (sawtooth+noise 380Hz, 0.85s — cigolìo basso), `mummy_grab` (triangle+noise 620Hz, 0.22s — presa sorda), `shabti_swing` (sawtooth+noise 800Hz, 0.38s — pietra oscilla), `shabti_charge` (triangle+noise 340Hz, 0.9s — rombo di pietra), `sobek_bite` (square+noise 1200Hz, 0.2s — morso rapido), `sobek_tail` (sine+noise 480Hz, 0.32s — colpo sordo), `royal_swing` (sawtooth+noise 900Hz, 0.5s, gain 0.17 — fendente pesante), `priest_dart` (triangle 1400→560Hz, 0.12s — fischio dardo), `stone_scrape` (sawtooth+noise 420Hz, 1.1s — pietra su pietra). I cue coprono tutti gli `audioCue` delle `AttackDef` in `enemies.ts`. Test: 4 nuovi in `tests/audio/ProceduralCueLibrary.test.ts` (tutti 11 validi gain>0+dur>0+freq>0; cue noise hanno noiseLowpassHz>0; `scarab_click` durata <0.1s; `royal_swing` gain > `scarab_click`). |
| 2026-08-17 | **B-03-alt: animazioni nemici procedurali (ThreeRendererService)** | ✅ **Idle breathing + attack lunge senza GLB rigged** | Alternativa procedurale a Mixamo (bloccato — login Adobe obbligatorio). `ThreeRendererService`: aggiunto `phaseOffset: number` all'array `enemyVisuals` (golden-ratio spacing: `enemyVisuals.length × 1.618`). In `setEnemyStates()`: per ogni nemico (1) **idle breathing** — sinusoide `breathHz = isAwake ? 1.15 : 0.45 Hz`, ampiezza `0.032 / 0.006 m`, traslazione verticale `idleY`; (2) **attack lunge** — `sin(telegraph × π) × 0.04 × telegraphGain` anticipa il colpo; (3) **breathScale** — modulazione lenta separata (×0.7 Hz, +1.2 rad) su scala. `performance.now()` usato solo nel renderer (conforme regola determinismo). B-03 originale (Mixamo, animazioni GLB rigged) rimane **BLOCCATO** — download manuale necessario. |
| 2026-08-17 | **B-02-alt: landmark geometry migliorata (ThreeDungeonLayout)** | ✅ **Sarcofago, obelisco, statua con mesh di dettaglio** | Alternativa procedurale a Sketchfab (bloccato — autenticazione obbligatoria). `ThreeDungeonLayout.buildLandmarks()` aggiunge mesh extra dopo il corpo principale — **Obelisco**: body `BoxGeometry(0.55,2.6,0.55)` + pyramidion `ConeGeometry(0.42,0.55,4)` dorato (mat 0xd4a05a, rot.Y=π/4, yOffset+1.35) + base `BoxGeometry(0.9,0.28,0.9)`; **Sarcofago**: body `BoxGeometry(1.9,0.75,0.95)` + coperchio dorato `BoxGeometry(1.88,0.22,0.93, yOffset+0.48)` + bordura `BoxGeometry(1.92,0.08,0.97, yOffset+0.37)`; **Statua**: body `CylinderGeometry(0.32,0.42,1.65,8)` + testa `SphereGeometry(0.28)` + nemes dorato `CylinderGeometry(0.34,0.38,0.1,12, yOffset+1.22)`. Tutti `castShadow=true`, materiali bronzo/oro/pietra coerenti con palette Egyptian Noir. Verificato in sorgente (ThreeDungeonLayout.ts 13311 byte). B-02 originale (Sketchfab GLB) rimane **BLOCCATO** — download manuale con autenticazione. |
| 2026-08-17 | **Asset bloccati: T2/S4/A1/A2/A4** | 📋 **NESSUNA RETE DISPONIBILE IN VM — azioni alternative documentate** | Tentativo di download automatico da VM cloud (Bash) e VM locale (device_bash): **nessuna delle due ha accesso internet**. Item bloccati definitivamente fino a download manuale: **T2** Poly Haven HDRI (`polyhaven.com/hdris` — `.hdr` file per `PMREMGenerator`), **S4** desert ambience OpenGameArt (`opengameart.org`, loop OGG CC0 per `setAmbienceLevel`), **A1** Quaternius Ruins CC0 (lightbox JS, niente URL diretto), **A2** ToxSam GLB registry (`github.com/ToxSam/open-source-3D-assets` — JSON manifest + GLB bulk), **A4** Mixamo animazioni (`mixamo.com`, login Adobe obbligatorio). Percorso per ciascuno: scarica dal browser → `scripts/optimize-assets.mjs` (meshopt) → aggiungi al manifest/asset library. Le alternative procedurali (B-02-alt, B-03-alt, B-05 drone, cue sintetici) coprono il VS; i download reali sono miglioramento post-VS. |
| 2026-08-16 | **Simulazione su Web Worker** | 📋 **RINVIATA (valutata, decisione registrata)** | **Valutazione**: spostare l'intera simulazione (ECS + Rapier + Director + runtime nemici) su un Web Worker richiederebbe (a) Rapier WASM già pronto per worker ma con `wasm-bindgen` init dedicato, (b) serializzazione del DomainEvent/state tra worker e main ad ogni tick (postMessage structured clone a 60Hz ≈ 1-3ms/frame solo di marshalling), (c) rifattorizzazione di `GameApplication` (oggi la logica di gioco è un'unica closure che tocca DOM/audio/renderer direttamente) in un'architettura command/query con eventi puri. **Rischio**: alto (rompe ogni sistema che legge `performance.now()`/DOM nel loop), **beneficio**: il loop è già leggero (~60 tick, budget Director 80 max, InstancedMesh ora taglia le draw call). **Decisione**: NON implementare nel vertical slice; la generazione procedurale (il carico pesante) è GIÀ su worker. Se un futuro profilo mostrerà il main thread saturo, il percorso è: estrarre `simulation.step` in un modulo worker-puro → `SimulationWorker` con protocollo JSON-RPC come `generation.protocol.ts`. |

---

## 4. VINCOLI DA RISPETTARE

1. **Licenze**: preferire CC0 (Quaternius, Kenney, ambientCG, Poly Haven, ancient-egypt-0); CC-BY richiede attribuzione nel README/credits.
2. **Zero-dip extra dove possibile**: GLTFLoader/DRACOLoader/postprocessing sono già in `three`; aggiungere dipendenze solo se necessarie (gltf-transform è CLI dev-only).
3. **Boundary architetturali** (verify-boundaries): content/ non importa three; il renderer consuma solo interfacce.
4. **Determinismo**: niente Math.random/Date.now in simulation/procedural/ai.
5. **Header JSDoc** su ogni nuovo file (Scopo/Ownership/Invarianti/Failure mode).
6. **Ogni passo chiude con `npm run verify` verde** prima di passare al successivo.

| 2026-08-26 | **Audit GAME-ART G-22→G-33** | 🔄 **Remediation code** | AssetLoader progress+singleton (G-27); AnimationStore ECS + Mixamo clip names (G-24); 32 archetipi + ProceduralDecorator + clue overlay (G-22/010/012/013); Recast navmesh + Yuka steering (G-28/G-32); RoomStreaming hop-BFS (G-31); HDRI desert (goegap_road CC0, file in public/hdri) + SSAO HIGH-only (G-25/G-29); ANUBIS_EXECUTIONER mesh (G-30); PBR aliases sandstone/limestone (G-33); ambience theme wind/drip (G-26); script PS1 Mixamo/HDRI/manifest. Mixamo clip FBX e pack Sonniss restano da scaricare (account/esterni). |
